# Local development defaults. Override in production.
DATABASE_URL="file:./dev.db"
JWT_SECRET="docgen-local-dev-secret-change-in-production"
PORT=4000

# Real GitHub OAuth (optional). Leave empty to use the built-in simulated flow.
# Register at github.com Settings > Developer settings > OAuth Apps, then paste:
GITHUB_CLIENT_ID="Ov23limuobOOseVAdBZt"
GITHUB_CLIENT_SECRET="3951ea9bc6107c04a207fdf08160af6c5bfffff5"
# Real GitLab / Bitbucket OAuth (optional, same pattern as GitHub):
GITLAB_CLIENT_ID=""
GITLAB_CLIENT_SECRET=""
BITBUCKET_CLIENT_ID=""
BITBUCKET_CLIENT_SECRET=""
# Where the browser is sent back after OAuth completes:
CLIENT_ORIGIN="http://localhost:5173"
OAUTH_REDIRECT_BASE="http://localhost:4000"
