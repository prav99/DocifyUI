{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install && npm install --prefix client && npm run build --prefix client && cd server && npm install"
  },
  "deploy": {
    "startCommand": "cd server && (npx prisma migrate deploy || true) && npm start",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 3
  }
}
